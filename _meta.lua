local _ = require("gettext")
return {
    name = "fastreader",
    fullname = _("FastReader"),
    description = _([[Display reading progress (current page and percentage) at the bottom of the screen.]]),
}
